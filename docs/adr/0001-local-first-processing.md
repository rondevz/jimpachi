# Local-First Recording and Processing

Jimpachi records audio, stores its SQLite history, transcribes with whisper.cpp, and generates summaries through a local Ollama instance on the user's machine. This accepts local setup and hardware constraints in exchange for privacy, offline operation, and user control over sensitive call recordings.
